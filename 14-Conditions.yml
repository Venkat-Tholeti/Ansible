---


- name : Conditions demo
  hosts: local
  connection: local
  vars:
      number: 4
  tasks:
      - name: Print message
        debug:
          msg: "Number is greater than 5"
        when: number > 5