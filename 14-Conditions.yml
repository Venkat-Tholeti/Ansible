---


- name : Conditions demo
  hosts: localhost
  connection: localhost
  vars:
      number: 10
  tasks:
      - name: Print message
        debug:
          msg: "Number is greater than 5"
        when: number > 5