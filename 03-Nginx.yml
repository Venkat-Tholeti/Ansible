---
  - name: Nginx Installation
    hosts: frontend
    become: yes
    tasks:
      - name: Install Nginx
        package:
           name: nginx
           state: present

      - name: start the nginx service
        service:
            name: nginx
            state: started
            enabled: yes
            