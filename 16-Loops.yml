---

- name: Loops
  hosts: local
  connection: local
  tasks:
    - name: Print names
      debug: 
       msg: "Hi ra {{ item }}" # item here is reserved key word
      loop:
      - Suresh
      - Ramesh
      - Joseph
      - Raheem

- name: Install Packages
  hosts: frontend
  become: yes
  tasks:
    - name: Install Packages
      package:
       name: "{{ item }}"
       state: installed
      loop:
         - mysql
         - zip
         - http